# Optional deploy hook — replace steps with your host (Vercel, Fly, Docker, etc.).
name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Configure deploy steps for your environment."
