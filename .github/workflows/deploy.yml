# Optional: wire your host (Vercel, Render, Fly, SSH to a VM). See DEPLOY.md in the repo root.
name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Hint
        run: echo "See DEPLOY.md — free paths use Oracle Always Free + Docker, or Vercel + self-hosted API."
