name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Install pnpm Globally
      run: npm install -g pnpm

    - name: Install Dependencies
      run: pnpm install

    - name: Build Frontend Application
      run: pnpm --prefix fe build

    - name: Build Backend Application
      run: pnpm --prefix be build

name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code from Git Repository
      uses: actions/checkout@v2

    - name: Set Up Node.js for CI/CD
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Globally Install pnpm
      run: npm install -g pnpm

    - name: Install All Project Dependencies
      run: pnpm install

    - name: Build the Frontend Application
      run: pnpm --prefix fe build

name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code from GitHub Repository
      uses: actions/checkout@v2

    - name: Set Up Node.js Environment for CI Process
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Globally Install pnpm Package Manager
      run: npm install -g pnpm

    - name: Install Project Dependencies Using pnpm
      run: pnpm install

    - name: Build the Frontend Application with pnpm
      run: pnpm --prefix fe build

    - name: Build the Backend Application with pnpm
      run: pnpm --prefix be build

    - name: Run Frontend Unit Tests with pnpm
      run: pnpm --prefix fe test

    - name: Run Backend Unit Tests with pnpm
      run: pnpm --prefix be test

    name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code from GitHub Repository
      uses: actions/checkout@v2

    - name: Set Up Node.js Environment for CI/CD Pipeline
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Install pnpm Globally
      run: npm install -g pnpm

    - name: Install Project Dependencies with pnpm
      run: pnpm install

    - name: Build Frontend Application Using pnpm
      run: pnpm --prefix fe build

    - name: Build Backend Application Using pnpm
      run: pnpm --prefix be build

    - name: Execute Frontend Unit Tests with pnpm
      run: pnpm --prefix fe test

    - name: Execute Backend Unit Tests with pnpm
      run: pnpm --prefix be test

name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 1. Checkout Code from GitHub Repository
      uses: actions/checkout@v2

    - name: 2. Set Up Node.js Environment for CI/CD Process
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: 3. Install pnpm Globally
      run: npm install -g pnpm

    - name: 4. Install All Project Dependencies Using pnpm
      run: pnpm install

    - name: 5. Build Frontend Application Using pnpm
      run: pnpm --prefix fe build

    - name: 6. Build Backend Application Using pnpm
      run: pnpm --prefix be build

    - name: 7. Run Frontend Unit Tests Using pnpm
      run: pnpm --prefix fe test

    - name: 8. Run Backend Unit Tests Using pnpm
      run: pnpm --prefix be test

    - name: 9. Start Deployment Process for the Application
      run: |
        echo 'Beginning deployment process...'
        # Add your deployment script here



    - name: Run Frontend Tests
      run: pnpm --prefix fe test

    name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code from GitHub
      uses: actions/checkout@v2

    - name: Set Up Node.js for CI
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Install pnpm Globally
      run: npm install -g pnpm

    - name: Install Project Dependencies with pnpm
      run: pnpm install

    - name: Build the Frontend Application
      run: pnpm --prefix fe build

    - name: Build the Backend Application
      run: pnpm --prefix be build

    - name: Run Frontend Unit Tests
      run: pnpm --prefix fe test

    - name: Run Backend Unit Tests
      run: pnpm --prefix be test

    - name: Application Deployment Process
      run: |
        echo 'Initiating the deployment process...'
        # Add your deployment script here

    - name: Deploy Project
      run: |
        echo 'Deploying application...'
        # Add your deployment script here


    - name: Build Backend
      run: pnpm --prefix be build

    - name: Run Tests
      run: |
        pnpm --prefix fe test
        pnpm --prefix be test

    - name: Deploy
      run: |
        echo 'Deploying application...'
        # Add your deployment script here
        
